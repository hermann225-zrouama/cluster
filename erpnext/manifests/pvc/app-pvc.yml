---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frappe-apps
  namespace: erp-dev
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi