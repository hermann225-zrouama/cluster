resources:
  - redis.yaml

configMapGenerator:
  - name: redis-config
    namespace: erp-dev
    files:
      - redis.conf
  - name: redis-queue-config
    namespace: erp-dev
    files:
      - queue.conf
  